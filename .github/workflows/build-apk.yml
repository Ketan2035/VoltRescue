name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: ☕ Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🟢 Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: client/package-lock.json

      - name: 📦 Install Client Dependencies
        working-directory: ./client
        run: npm ci || npm install

      - name: ⚙️ Generate Native Android Project (Expo Prebuild)
        working-directory: ./client
        run: npx expo prebuild --platform android --clean

      - name: 🔑 Configure Android Signing for Direct Sideloading
        working-directory: ./client/android
        run: |
          sed -i 's/signingConfig signingConfigs.release/signingConfig signingConfigs.debug/g' app/build.gradle || true

      - name: 🔨 Build Debug & Release APKs via Gradle
        working-directory: ./client/android
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon

      - name: 🚀 Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: VoltRescue-Android-APK
          path: client/android/app/build/outputs/apk/debug/*.apk
